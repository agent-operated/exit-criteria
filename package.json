{
  "name": "exit-criteria",
  "version": "0.0.0-development",
  "description": "Bind GTP done_conditions to executable checks and let a machine decide",
  "private": true,
  "type": "module",
  "bin": {
    "exit-criteria": "dist/src/cli.js"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --test dist/test/unit/*.test.js dist/test/e2e/*.test.js",
    "exit-criteria": "node dist/src/cli.js"
  },
  "dependencies": {
    "yaml": "2.9.0"
  },
  "devDependencies": {
    "@types/node": "26.1.1",
    "typescript": "7.0.2"
  }
}
